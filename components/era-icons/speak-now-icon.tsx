export default function SpeakNowIcon({ className }: { className?: string }) {
  return (
    <div
      className={`${className} bg-gradient-to-br from-purple-400 to-purple-700 rounded-lg flex items-center justify-center font-bold text-white text-xs shadow-md`}
    >
      <span>SPEAK NOW</span>
    </div>
  )
}
