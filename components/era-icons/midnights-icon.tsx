export default function MidnightsIcon({ className }: { className?: string }) {
  return (
    <div
      className={`${className} bg-gradient-to-br from-indigo-900 to-purple-900 rounded-lg flex items-center justify-center font-bold text-blue-200 text-xs shadow-md`}
    >
      <span>MIDNIGHTS</span>
    </div>
  )
}
