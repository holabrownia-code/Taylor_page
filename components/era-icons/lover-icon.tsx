export default function LoverIcon({ className }: { className?: string }) {
  return (
    <div
      className={`${className} bg-gradient-to-br from-pink-300 via-purple-300 to-blue-300 rounded-lg flex items-center justify-center font-bold text-pink-700 text-xs shadow-md`}
    >
      <span>LOVER</span>
    </div>
  )
}
