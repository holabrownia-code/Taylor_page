export default function FearlessIcon({ className }: { className?: string }) {
  return (
    <div
      className={`${className} bg-gradient-to-br from-yellow-300 to-yellow-600 rounded-lg flex items-center justify-center font-bold text-white text-xs shadow-md`}
    >
      <span>FEARLESS</span>
    </div>
  )
}
