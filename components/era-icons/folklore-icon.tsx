export default function FolkloreIcon({ className }: { className?: string }) {
  return (
    <div
      className={`${className} bg-gradient-to-br from-gray-300 to-gray-500 rounded-lg flex items-center justify-center font-bold text-white text-xs shadow-md`}
    >
      <span>FOLKLORE</span>
    </div>
  )
}
