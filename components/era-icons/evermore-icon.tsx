export default function EvermoreIcon({ className }: { className?: string }) {
  return (
    <div
      className={`${className} bg-gradient-to-br from-orange-700 to-amber-900 rounded-lg flex items-center justify-center font-bold text-orange-100 text-xs shadow-md`}
    >
      <span>EVERMORE</span>
    </div>
  )
}
